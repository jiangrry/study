bind()：
功能：
    绑定地址
参数：
    一个元祖包含两项, 第一项为 IP, 第二项为Port 端口

第一个参数：
    1. "localhost"、"127.0.0.1"  --> 只能在自己主机上去访问服务端(127.0.0.1)
    2. ip 地址： ifconfig(linux|mac) | ipconfig(windows)
        说明可以被网络访问, 公网IP 和 私网IP
    3. "" 、 "0.0.0.0" 表示既可以被别的主机访问, 也可以自己访问
学习阶段：
    使用局域网

